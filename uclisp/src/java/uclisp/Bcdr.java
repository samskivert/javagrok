//
// Bcdr - a builtin function to return the tail of a list

package uclisp;

public class Bcdr extends Function
{
    /*
     * TASK 4
     * ======
     * 
     * Welcome to the fourth task.
     * 
     * You're fourth task is to implement the 'cdr' function
     * which returns the tail of the given list.
     * 
     * cons takes exactly one argument: a list
     * 
     * examples: (cdr (cons 3 nil)) --> nil
     *           (cdr nil) --> nil
     *           (cdr (cons 2 (cons 3 (cons 1 nil)))) --> (3 1)
     * 
     * At the end you should be able to run: ant cdr
     * which will run the lisp/cdr.lisp program.
     * 
     */
}
