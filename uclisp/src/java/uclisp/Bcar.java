//
// Bcar - a builtin function to return the head of a list

package uclisp;

public class Bcar extends Function
{
    /*
     * TASK 3
     * ======
     * 
     * Welcome to the third task.
     * 
     * You're third task is to implement the 'car' function which returns the
     * head of the given list.
     * 
     * car takes exactly one argument: a list
     * 
     * examples: (car (cons 3 nil)) --> 3
     *           (car nil) --> nil
     *           (car (cons 2 (cons 3 (cons 1 nil)))) --> 2
     * 
     * At the end you should be able to run: ant car
     * which will run the lisp/car.lisp program.
     * 
     */
}
